<!DOCTYPE html>
<html lang="ar" dir="rtl">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="icon" type="image/png" href="Untitled_design-removebg-preview.png">
  <title>Afnan Haute Craft - تسجيل الدخول</title>

  <link href="https://fonts.googleapis.com/css2?family=Cairo:wght@400;700&display=swap" rel="stylesheet">

  <style>
    body {
      margin: 0;
      padding: 0;
      height: 100vh;
      display: flex;
      flex-direction: column;
      justify-content: center;
      align-items: center;
      background: linear-gradient(135deg, #6a00f4, #b5179e, #7209b7, #f72585);
      background-size: 300% 300%;
      animation: gradientMove 10s ease infinite;
      font-family: 'Cairo', sans-serif;
      color: #fff;
      text-align: center;
      overflow: hidden;
    }

    @keyframes gradientMove {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }

    .logo {
      width: 200px;
      max-width: 80%;
      margin-bottom: 25px;
      border-radius: 20px;
      animation: fadeInDown 1.5s ease forwards;
      filter: drop-shadow(0 0 10px rgba(255, 255, 255, 0.6));
    }

    .container {
      background: rgba(0, 0, 0, 0.35);
      padding: 50px 35px;
      border-radius: 25px;
      box-shadow: 0 15px 40px rgba(0, 0, 0, 0.3);
      width: 92%;
      max-width: 520px;
      transform: translateY(50px);
      opacity: 0;
      animation: fadeInUp 2s ease forwards 0.5s;
      backdrop-filter: blur(10px);
    }

    h1 {
      font-size: 36px;
      margin-bottom: 25px;
      font-weight: 700;
    }

    p {
      font-size: 22px;
      margin-bottom: 25px;
      line-height: 1.9;
    }

    input[type="password"] {
      width: 80%;
      padding: 15px;
      border: none;
      border-radius: 12px;
      font-size: 20px;
      margin-bottom: 25px;
      text-align: center;
      outline: none;
    }

    button {
      padding: 18px 55px;
      background: linear-gradient(90deg, #b5179e, #7209b7);
      color: #fff;
      border: none;
      border-radius: 12px;
      font-size: 22px;
      cursor: pointer;
      font-weight: bold;
      transition: 0.3s;
    }

    button:hover {
      transform: scale(1.07);
      box-shadow: 0 8px 30px rgba(114, 9, 183, 0.7);
    }

    @keyframes fadeInUp {
      0% { opacity: 0; transform: translateY(50px); }
      100% { opacity: 1; transform: translateY(0); }
    }

    @keyframes fadeInDown {
      0% { opacity: 0; transform: translateY(-40px); }
      100% { opacity: 1; transform: translateY(0); }
    }
  </style>
</head>

<body>
  <img src="https://i.postimg.cc/NGDRjRzL/Untitled-design-removebg-preview.png" class="logo" alt="Logo">

  <div class="container">
    <h1>تسجيل الدخول</h1>
    <p>الرجاء إدخال كلمة المرور للمتابعة</p>

    <input type="password" id="password" placeholder="••••••••••">
    <button onclick="checkPassword()">دخول</button>
    
    <p id="error" style="color: #ffb3cc; font-size: 18px; margin-top: 20px;"></p>
  </div>

  <script>
    function checkPassword() {
      const pass = document.getElementById("password").value;
      const correct = "Afnan@13@Zk";

      if (pass === correct) {
        window.location.href = "https://sites.google.com/view/afnantlm/";
      } else {
        document.getElementById("error").innerText = "كلمة المرور غير صحيحة";
      }
    }
  </script>

</body>
</html>
